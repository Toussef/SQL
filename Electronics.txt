CREATE TABLE  "ELECTRONICS" 
   (	"CUSTOMSSTAT" VARCHAR2(6) NOT NULL ENABLE, 
	"BARCODE" NUMBER(12,0), 
	"PRICE" NUMBER(*,0), 
	"QUANTITY" NUMBER(*,0), 
	"CATEGORY" VARCHAR2(25), 
	"DOM" NUMBER(*,0), 
	"BATTERYSTAT" VARCHAR2(6) NOT NULL ENABLE, 
	"NAME" VARCHAR2(50) NOT NULL ENABLE, 
	 PRIMARY KEY ("BARCODE") ENABLE
   ) ;
