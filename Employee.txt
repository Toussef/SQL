CREATE TABLE  "EMPLOYEE" 
   (	ID CHAR(9) NOT NULL ENABLE, 
	FIRSTNAME VARCHAR2(15) NOT NULL ENABLE, 
	LASTNAME VARCHAR2(15) NOT NULL ENABLE, 
	DATEOFBIRTH DATE, 
	ADDRESS VARCHAR2(15), 
	POSITION VARCHAR2(15) NOT NULL ENABLE, 
	SALARY NUMBER(6,2) NOT NULL ENABLE,
	 PRIMARY KEY (ID) ENABLE
   ) ;
