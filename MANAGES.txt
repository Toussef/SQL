CREATE TABLE  "MANAGES" 
   (	"COUNTRY" VARCHAR2(25) NOT NULL ENABLE, 
	"CITY" VARCHAR2(25) NOT NULL ENABLE, 
	"STREET" VARCHAR2(25) NOT NULL ENABLE, 
	"BRANCHNUM" NUMBER(4,0), 
	"NBOFEMPS" NUMBER(*,0) NOT NULL ENABLE, 
	"MDEPTNUM" NUMBER(4,0), 
	 PRIMARY KEY ("BRANCHNUM", "MDEPTNUM") ENABLE
   ) ;ALTER TABLE  "MANAGES" ADD CONSTRAINT "MANAGES_FK1" FOREIGN KEY ("BRANCHNUM")
	  REFERENCES  "BRANCH" ("Number") ENABLE;ALTER TABLE  "MANAGES" ADD CONSTRAINT "MANAGES_FK2" FOREIGN KEY ("MDEPTNUM")
	  REFERENCES  "MANAGING_DEPT" ("NUMBER") ENABLE;

