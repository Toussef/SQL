CREATE TABLE  "GOODS" 
   (	"QUANTITY" NUMBER(38,0) NOT NULL ENABLE, 
	"DAYSTOEXPIRE" NUMBER(*,0), 
	"BARCODE" NUMBER(12,0), 
	"STATUS" VARCHAR2(5) NOT NULL ENABLE, 
	"COOLING" VARCHAR2(5) NOT NULL ENABLE, 
	"PRICE" FLOAT(126), 
	"NAME" VARCHAR2(50) NOT NULL ENABLE, 
	 PRIMARY KEY ("BARCODE") ENABLE
   ) ;

